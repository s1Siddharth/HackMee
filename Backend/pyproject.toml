[project]
name = "datathon"
version = "0.1.0"
description = "Automated Insight Analyst - Datathon 2026"
requires-python = ">=3.11"
dependencies = [
    "streamlit",
    "streamlit-float",
    "pandas",
    "numpy",
    "openpyxl",
    "plotly",
    "groq",
    "python-dotenv",
    "scikit-learn",
    "fastapi",
    "uvicorn[standard]",
    "python-multipart",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src"]
